 {%- for block in section.blocks -%}

{%- endfor -%}


<div class="max-w-7xl mx-auto mb-25 divide-y divide-outline overflow-hidden rounded-radius rounded-md border border-gold-400 border-outline bg-surface-alt/40 text-on-surface dark:divide-outline-dark dark:border-outline-dark dark:bg-surface-dark-alt/50 dark:text-on-surface-dark">
  {%- for block in section.blocks -%}
    {% if section.settings.open_first_collapsible_row and forloop.first %}
      <div x-data="{ isExpanded: false }">
        <button id="controlsAccordionItem{{ block.settings.content_number }}" type="button" class="flex w-full items-center justify-between gap-4 bg-surface-alt p-4 text-left underline-offset-2 hover:bg-surface-alt/75 focus-visible:bg-surface-alt/75 focus-visible:underline focus-visible:outline-hidden dark:bg-surface-dark-alt dark:hover:bg-surface-dark-alt/75 dark:focus-visible:bg-surface-dark-alt/75" aria-controls="accordionItemOne" x-on:click="isExpanded = ! isExpanded" x-bind:class="isExpanded ? 'text-on-surface-strong dark:text-on-surface-dark-strong font-bold'  : 'text-on-surface dark:text-on-surface-dark font-medium'" x-bind:aria-expanded="isExpanded ? 'true' : 'false'">
          {{ block.settings.content_number }} {{ block.settings.heading }}
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke="currentColor" class="size-5 shrink-0 transition" aria-hidden="true" x-bind:class="isExpanded  ?  'rotate-180'  :  ''">
               <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5"/>
            </svg>
        </button>
          <div x-cloak x-show="isExpanded" id="accordionItem{{ block.settings.content_number }}" role="region" aria-labelledby="controlsAccordionItemOne" x-collapse>
            <div class="p-4 text-sm sm:text-base text-pretty">
                {{ block.settings.row_content }}
            </div>
        </div>
        </div>
      {% else %}
      <div x-data="{ isExpanded: false }">
        <button id="controlsAccordionItem{{ block.settings.content_number }}" type="button" class="flex w-full items-center justify-between gap-4 bg-surface-alt p-4 text-left underline-offset-2 hover:bg-surface-alt/75 focus-visible:bg-surface-alt/75 focus-visible:underline focus-visible:outline-hidden dark:bg-surface-dark-alt dark:hover:bg-surface-dark-alt/75 dark:focus-visible:bg-surface-dark-alt/75" aria-controls="accordionItemOne" x-on:click="isExpanded = ! isExpanded" x-bind:class="isExpanded ? 'text-on-surface-strong dark:text-on-surface-dark-strong font-bold'  : 'text-on-surface dark:text-on-surface-dark font-medium'" x-bind:aria-expanded="isExpanded ? 'true' : 'false'">
        <div class="text-lg mr-10">{{ block.settings.content_number }}</div> {{ block.settings.heading }}
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke="currentColor" class="size-5 shrink-0 transition" aria-hidden="true" x-bind:class="isExpanded  ?  'rotate-180'  :  ''">
               <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5"/>
            </svg>
        </button>
          <div x-cloak x-show="isExpanded" id="accordionItem{{ block.settings.content_number }}" role="region" aria-labelledby="controlsAccordionItemOne" x-collapse>
            <div class="p-4 mx-10 text-sm sm:text-base text-pretty">
                {{ block.settings.row_content }}
            </div>
        </div>
        </div>
    {% endif %}
  {%- endfor -%}
</div>

<div class="max-w-7xl mx-auto px-1 md:px-8 mb-20">
  {% if section.settings.further_text != blank or section.settings.contact_btn_text != blank %}
    <div class="further-questions" style="text-align: {{ section.settings.main_title_alignment }};">
      <p class="further-paragraph">{{ section.settings.further_text }}</p>
      <a {% if section.settings.contact_btn_link %} href="{{ section.settings.contact_btn_link }}" {% else %} role="link" aria-disabled="true" {% endif %} class="contact-faq-btn theme_buttons button">{{ section.settings.contact_btn_text }}</a>
    </div>
  {% endif %}
</div>


{% schema %}
{
  "name": "t:sections.collapsible_content.name",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "checkbox",
      "id": "show_topimg",
      "label": "Show top image",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "faq_top_img",
      "label": "Image"
    },
    {
      "type": "checkbox",
      "id": "open_first_collapsible_row",
      "default": false,
      "label": "t:sections.collapsible_content.settings.open_first_collapsible_row.label"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "t:sections.rich-text.settings.full_width.label",
      "default": false
    },
    {
      "type": "header",
      "content": "t:sections.collapsible_content.settings.header.content"
    },
    {
      "type": "header",
      "content": "Contact settings"
    },
    {
      "type": "text",
      "id": "further_text",
      "default": "Still have questions? contact us. ",
      "label": "Contact text"
    },
    {
      "type": "text",
      "id": "contact_btn_text",
      "default": "Contact",
      "label": "Button text"
    },
    {
      "type": "url",
      "id": "contact_btn_link",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "t:sections.social_media_collage.settings.color_setting"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    }
  ],
  "blocks": [
    {
      "type": "collapsible_row",
      "name": "t:sections.collapsible_content.blocks.collapsible_row.name",
      "settings": [
        {
          "type": "text",
          "id": "content_number",
          "default": "01",
          "label": "t:sections.social_media_collage.settings.number"
        },
        {
          "type": "textarea",
          "id": "heading",
          "default": "Frequently ask question",
          "label": "t:sections.collapsible_content.blocks.collapsible_row.settings.heading.label",
          "info": "t:sections.collapsible_content.blocks.collapsible_row.settings.heading.info"
        },
        {
          "type": "textarea",
          "id": "row_content",
          "default": "Shopify is a complete commerce platform that lets you start, grow, and manage a business.Create and customize an online store Sell in multiple places, including web, mobile, social media, online marketplaces, brick-and-mortar locations, and pop-up shops.",
          "label": "t:sections.collapsible_content.blocks.collapsible_row.settings.row_content.label"
        },
        {
          "type": "richtext",
          "id": "richtext",
          "label": "Richtext"
        }
      ]
    }
  ]
}
{% endschema %}
